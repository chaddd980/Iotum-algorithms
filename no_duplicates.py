# Question:
# Given: words = ['one', 'one', 'two', 'three', 'three', 'two']
# Remove the duplicates.
# Language of choice = python
# i've never used python before, just decided to read up on it enough to
# do this challenge since it's one of the languages required 
def no_duplicates(words):
  print list(set(words))
  return;

no_duplicates( ['one', 'one', 'two', 'three', 'three', 'two'])
